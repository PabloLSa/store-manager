const { getAllproducts, productById, insertProducts } = require('./product.model');
const salesModel = require('./sales.model');

module.exports = { getAllproducts, productById, insertProducts, salesModel };